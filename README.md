# React + Vite + Storybook + Antd

This repo is created to hold test react components made with antd. It uses storybook to visualise and test these components.

## Install
npm i

## Run Storybook
npm run storybook